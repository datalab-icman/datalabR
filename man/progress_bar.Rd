% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ProgressBar.R
\name{progress_bar}
\alias{progress_bar}
\title{Create a progress bar for loops and iterations}
\usage{
progress_bar(
  total = 100,
  format = "{bar} {percent} {eta}",
  style = "modern",
  ...
)
}
\arguments{
\item{total}{Total number of iterations}

\item{format}{Format string with tokens (default: "{bar} {percent} {eta}")}

\item{style}{Visual style (default: "modern")}

\item{...}{Additional arguments passed to \code{ProgressBar$new()}}
}
\value{
A \code{ProgressBar} object
}
\description{
A convenience function to create a colorful progress bar with Unicode symbols.
This is a simple wrapper around \code{ProgressBar$new()} with sensible defaults.
}
\examples{
\dontrun{
# Simple loop with progress bar
pb <- progress_bar(total = 100)
for (i in 1:100) {
  pb$tick()
  Sys.sleep(0.02)
}

# Custom style and format
pb <- progress_bar(
  total = 50,
  format = "{spin} Working {bar} {percent}",
  style = "elegant"
)
for (i in 1:50) {
  pb$tick()
  Sys.sleep(0.05)
}

# With status messages
pb <- progress_bar(
  total = 30,
  format = "{bar} {current}/{total} | {msg}",
  style = "circles"
)
for (i in 1:30) {
  pb$tick(tokens = list(msg = paste("Processing item", i)))
  Sys.sleep(0.1)
}
}

}
