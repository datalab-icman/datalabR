% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ProgressBar.R
\name{progress_parallel}
\alias{progress_parallel}
\title{Progress bar for parallel operations}
\usage{
progress_parallel(
  X,
  FUN,
  ...,
  .mc.cores = getOption("mc.cores", 2L),
  .format = "{bar} {percent} {eta}",
  .style = "modern",
  .method = c("fork", "psock")
)
}
\arguments{
\item{X}{A vector or list to iterate over}

\item{FUN}{Function to apply to each element}

\item{...}{Additional arguments passed to FUN}

\item{.mc.cores}{Number of cores to use (Unix-like systems only)}

\item{.format}{Format string for the progress bar}

\item{.style}{Visual style of the progress bar}

\item{.method}{Method to use: "fork" (Unix) or "psock" (Windows/Unix)}
}
\value{
A list of results from applying FUN to each element of X
}
\description{
A wrapper around \code{parallel::mclapply()} or \code{parallel::parLapply()} that shows
a progress bar. This function provides visual feedback for parallel processing
operations.
}
\examples{
\dontrun{
# Parallel processing with progress (Unix-like systems)
results <- progress_parallel(
  1:100,
  function(x) {
    Sys.sleep(0.1)
    x^2
  },
  .mc.cores = 4,
  .format = "{spin} {bar} {percent} | {current}/{total}",
  .style = "modern"
)

# Works on all platforms with PSOCK method
results <- progress_parallel(
  large_dataset,
  expensive_function,
  .method = "psock",
  .mc.cores = 4,
  .style = "elegant"
)
}

}
