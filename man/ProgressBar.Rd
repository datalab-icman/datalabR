% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ProgressBar.R
\name{ProgressBar}
\alias{ProgressBar}
\title{Progress bar class with colored Unicode symbols}
\description{
A comprehensive and visually appealing progress bar implementation with colored
Unicode symbols, spinners, and rich formatting options. Inspired by the cli package
but standalone and fully self-contained.
}
\details{
This progress bar system provides:
\itemize{
\item Beautiful Unicode box-drawing characters and symbols
\item ANSI color support with automatic terminal detection
\item Multiple built-in styles (modern, classic, elegant, minimal, dots, arrows)
\item Customizable colors for complete, current, and incomplete sections
\item Animated spinners with various styles
\item Automatic ETA and rate calculations
\item Terminal width detection and responsive layout
\item Clean completion and interruption handling
}
}
\section{Format tokens}{

The following tokens can be used in the format string:
\itemize{
\item \code{{bar}} - The progress bar itself with colored symbols
\item \code{{current}} - Current iteration number
\item \code{{total}} - Total number of iterations
\item \code{{percent}} - Percentage complete (e.g., "45\%")
\item \code{{elapsed}} - Time elapsed since start
\item \code{{eta}} - Estimated time to completion
\item \code{{rate}} - Iterations per second
\item \code{{spin}} - Animated spinning indicator
\item \code{{status}} - Custom status message
\item Any custom token passed via the \code{tokens} parameter
}
}

\section{Color support}{

Colors are automatically enabled if the terminal supports ANSI colors.
You can force enable/disable colors with the \code{use_colors} parameter.
Available colors: black, red, green, yellow, blue, magenta, cyan, white,
and their bright variants (e.g., "bright_green").
}

\section{Built-in styles}{

\itemize{
\item \code{modern} - Colorful boxes with smooth gradients (default)
\item \code{classic} - Traditional equal signs and dashes
\item \code{elegant} - Unicode block elements with subtle colors
\item \code{minimal} - Simple dots, clean and fast
\item \code{dots} - Circular dots that fill up
\item \code{arrows} - Arrow symbols pointing right
\item \code{blocks} - Solid and shaded Unicode blocks
\item \code{circles} - Circular progress indicators
}
}

\examples{
\dontrun{
# Basic usage with default modern style
pb <- ProgressBar$new(total = 100)
for (i in 1:100) {
  pb$tick()
  Sys.sleep(0.02)
}

# Different styles showcase
styles <- c("modern", "elegant", "dots", "arrows", "blocks", "circles")
for (style in styles) {
  cat("\nStyle:", style, "\n")
  pb <- ProgressBar$new(
    total = 50,
    format = "{spin} {bar} {percent} | {style}",
    style = style,
    width = 40
  )
  for (i in 1:50) {
    pb$tick(tokens = list(style = style))
    Sys.sleep(0.01)
  }
}

# Custom colors and format
pb <- ProgressBar$new(
  total = 100,
  format = "{spin} Processing {bar} {percent} | ETA: {eta}",
  complete_color = "bright_green",
  current_color = "bright_yellow",
  incomplete_color = "bright_black",
  width = 60
)
for (i in 1:100) {
  pb$tick()
  Sys.sleep(0.02)
}

# File processing with custom tokens
files <- paste0("data_file_", 1:25, ".csv")
pb <- ProgressBar$new(
  total = length(files),
  format = "{spin} {bar} {current}/{total} | Processing: {filename}",
  style = "elegant",
  show_after = 0
)
for (f in files) {
  # Simulate file processing
  pb$tick(tokens = list(filename = f))
  Sys.sleep(0.1)
}

# Data processing with status updates
pb <- ProgressBar$new(
  total = 200,
  format = "{spin} {bar} {percent} | {status}",
  style = "blocks"
)
for (i in 1:200) {
  status <- if (i \%\% 50 == 0) "Checkpoint saved" else "Processing..."
  pb$tick(tokens = list(status = status))
  Sys.sleep(0.01)
}

# Minimal style for fast operations
pb <- ProgressBar$new(
  total = 1000,
  format = "{bar} {current}/{total} {rate}",
  style = "minimal",
  clear = FALSE
)
for (i in 1:1000) {
  pb$tick()
  Sys.sleep(0.001)
}

# Long-running task with detailed info
pb <- ProgressBar$new(
  total = 500,
  format = paste0(
    "{spin} Analysis {bar} {percent}\n",
    "├─ Completed: {current}/{total} items\n",
    "├─ Speed: {rate}\n",
    "└─ ETA: {eta}"
  ),
  style = "modern",
  width = 50
)
for (i in 1:500) {
  pb$tick()
  Sys.sleep(0.005)
}

# Using update() instead of tick()
pb <- ProgressBar$new(total = 100, style = "circles")
for (i in seq(0, 100, by = 5)) {
  pb$update(i, tokens = list(status = paste("Step", i)))
  Sys.sleep(0.1)
}

# Downloading simulation
pb <- ProgressBar$new(
  total = 100,
  format = "{spin} Downloading {bar} {percent} | {speed} MB/s | {eta}",
  style = "arrows"
)
for (i in 1:100) {
  speed <- round(runif(1, 1.5, 5.5), 1)
  pb$tick(tokens = list(speed = speed))
  Sys.sleep(0.03)
}
}

}
\section{Public fields}{
\if{html}{\out{<div class="r6-fields">}}
\describe{
\item{\code{total}}{Total number of ticks}

\item{\code{current}}{Current tick count}

\item{\code{width}}{Width of the progress bar}

\item{\code{format}}{Format string for display}

\item{\code{clear}}{Whether to clear the progress bar on completion}

\item{\code{show_after}}{Show progress bar after this many seconds}

\item{\code{style}}{Visual style of the progress bar}
}
\if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-ProgressBar-new}{\code{ProgressBar$new()}}
\item \href{#method-ProgressBar-tick}{\code{ProgressBar$tick()}}
\item \href{#method-ProgressBar-update}{\code{ProgressBar$update()}}
\item \href{#method-ProgressBar-terminate}{\code{ProgressBar$terminate()}}
\item \href{#method-ProgressBar-ratio}{\code{ProgressBar$ratio()}}
\item \href{#method-ProgressBar-elapsed}{\code{ProgressBar$elapsed()}}
\item \href{#method-ProgressBar-set_style}{\code{ProgressBar$set_style()}}
\item \href{#method-ProgressBar-clone}{\code{ProgressBar$clone()}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-ProgressBar-new"></a>}}
\if{latex}{\out{\hypertarget{method-ProgressBar-new}{}}}
\subsection{Method \code{new()}}{
Create a new progress bar with colored Unicode symbols
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{ProgressBar$new(
  total = 100,
  format = "{bar} {percent} {eta}",
  width = NULL,
  style = "modern",
  complete_color = "green",
  current_color = "yellow",
  incomplete_color = "white",
  current = 0,
  clear = TRUE,
  show_after = 0.2,
  use_colors = NULL,
  force = FALSE,
  stream = stderr()
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{total}}{Total number of iterations}

\item{\code{format}}{Format string with tokens (default: "{bar} {percent} {eta}")}

\item{\code{width}}{Width of the progress bar in characters (default: auto-detect)}

\item{\code{style}}{Visual style: "modern", "classic", "elegant", "minimal",
"dots", "arrows", "blocks", or "circles" (default: "modern")}

\item{\code{complete_color}}{Color for completed portion (default: "green")}

\item{\code{current_color}}{Color for the current position indicator (default: "yellow")}

\item{\code{incomplete_color}}{Color for incomplete portion (default: "white")}

\item{\code{current}}{Starting position (default: 0)}

\item{\code{clear}}{Clear bar on completion (default: TRUE)}

\item{\code{show_after}}{Delay in seconds before showing (default: 0.2)}

\item{\code{use_colors}}{Force enable/disable colors (default: auto-detect)}

\item{\code{force}}{Force display in non-interactive sessions (default: FALSE)}

\item{\code{stream}}{Output stream (default: stderr())}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
A new \code{ProgressBar} object
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-ProgressBar-tick"></a>}}
\if{latex}{\out{\hypertarget{method-ProgressBar-tick}{}}}
\subsection{Method \code{tick()}}{
Increment the progress bar by one or more steps
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{ProgressBar$tick(len = 1, tokens = list())}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{len}}{Number of steps to advance (default: 1)}

\item{\code{tokens}}{Named list of custom tokens to display}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
Invisible self for method chaining
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-ProgressBar-update"></a>}}
\if{latex}{\out{\hypertarget{method-ProgressBar-update}{}}}
\subsection{Method \code{update()}}{
Update the progress bar to a specific position
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{ProgressBar$update(current, tokens = list())}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{current}}{New current position (0 to total)}

\item{\code{tokens}}{Named list of custom tokens to display}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
Invisible self for method chaining
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-ProgressBar-terminate"></a>}}
\if{latex}{\out{\hypertarget{method-ProgressBar-terminate}{}}}
\subsection{Method \code{terminate()}}{
Mark the progress bar as complete and clean up
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{ProgressBar$terminate(tokens = list())}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{tokens}}{Named list of custom tokens for final display}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
Invisible self
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-ProgressBar-ratio"></a>}}
\if{latex}{\out{\hypertarget{method-ProgressBar-ratio}{}}}
\subsection{Method \code{ratio()}}{
Get the current progress as a ratio between 0 and 1
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{ProgressBar$ratio()}\if{html}{\out{</div>}}
}

\subsection{Returns}{
Numeric value between 0 and 1
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-ProgressBar-elapsed"></a>}}
\if{latex}{\out{\hypertarget{method-ProgressBar-elapsed}{}}}
\subsection{Method \code{elapsed()}}{
Get elapsed time since progress bar creation
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{ProgressBar$elapsed()}\if{html}{\out{</div>}}
}

\subsection{Returns}{
Numeric elapsed time in seconds
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-ProgressBar-set_style"></a>}}
\if{latex}{\out{\hypertarget{method-ProgressBar-set_style}{}}}
\subsection{Method \code{set_style()}}{
Change the style of the progress bar on the fly
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{ProgressBar$set_style(style)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{style}}{New style name}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
Invisible self
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-ProgressBar-clone"></a>}}
\if{latex}{\out{\hypertarget{method-ProgressBar-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{ProgressBar$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}
